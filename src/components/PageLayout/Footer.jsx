import PropTypes from 'prop-types';
import styles from './pageLayout.module.scss';

const Footer = () => (
  <footer className={styles.mainFooter}>Footer Place Holder!</footer>
);

Footer.defaultProps = {

};

Footer.propTypes = {

};

export default Footer;
